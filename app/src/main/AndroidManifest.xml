<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agoto.IPAlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="@string/app_name" android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IPAtableActivity" android:screenOrientation="landscape" />
        <activity android:name=".haikiryuActivity" android:screenOrientation="landscape" />
        <activity android:name=".hihaikiryuActivity" android:screenOrientation="landscape"/>
        <activity android:name=".boinActivity" android:screenOrientation="landscape"/>
        <activity android:name=".otherActivity" android:screenOrientation="landscape"/>
    </application>

</manifest>